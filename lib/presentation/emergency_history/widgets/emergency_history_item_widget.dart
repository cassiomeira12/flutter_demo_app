import 'package:dependency/dependency.dart';

class EmergencyHistoryItemWidget extends StatelessWidget {
  const EmergencyHistoryItemWidget({super.key});

  @override
  Widget build(BuildContext context) {
    return const Placeholder();
  }
}
