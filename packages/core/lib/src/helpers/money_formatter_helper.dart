// import 'package:money_formatter/money_formatter.dart';

abstract class MoneyFormatterHelper {
  static String format(double value) {
    // Locale? currentLocale = Get.locale;

    // MoneyFormatterSettings? formatterSettings;

    // if (currentLocale?.toString() == 'pt_BR') {
    //   formatterSettings = MoneyFormatterSettings(
    //     symbol: r'R$',
    //     fractionDigits: 2,
    //     decimalSeparator: ',',
    //     thousandSeparator: '.',
    //     compactFormatType: CompactFormatType.short,
    //   );
    // }

    // var formatter = MoneyFormatter(
    //   amount: value,
    //   settings: formatterSettings,
    // );

    // MoneyFormatterOutput formatted = formatter.output;

    // return formatted.symbolOnLeft;

    return '';
  }
}
