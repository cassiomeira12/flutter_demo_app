name: firebase_initialize
description: "A new Flutter package project."
version: 0.0.1
homepage:
publish_to: "none"
resolution: workspace

environment:
  sdk: ^3.10.1
  flutter: ">=3.38.3"

dependencies:
  flutter:
    sdk: flutter

  core:
    path: ../core
  dependency:
    path: ../dependency

  firebase_core: 4.2.1

dev_dependencies:
  flutter_test:
    sdk: flutter

flutter:
