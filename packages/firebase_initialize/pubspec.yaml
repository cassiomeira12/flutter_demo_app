name: firebase_initialize
description: "A new Flutter package project."
version: 0.0.1
homepage:
publish_to: "none"
resolution: workspace

environment:
  sdk: ^3.12.0
  flutter: ">=3.44.0"

dependencies:
  flutter:
    sdk: flutter

  core:
    path: ../core
  dependency:
    path: ../dependency

  firebase_core: 4.10.0 # must update pod install

dev_dependencies:
  flutter_test:
    sdk: flutter

flutter:
  uses-material-design: false
